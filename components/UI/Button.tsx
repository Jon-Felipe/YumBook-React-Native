import { StyleSheet, Button as UIButton } from 'react-native';
import React from 'react';

export default function Button() {
  return <UIButton title='Click Me' />;
}

const styles = StyleSheet.create({});
